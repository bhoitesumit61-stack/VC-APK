name: voxlink_apk
description: VoxLink - Premium Voice Chat Application
version: 1.0.0+1
publish_to: 'none'

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  livekit_client: ^2.1.9
  permission_handler: ^11.3.1
  google_fonts: ^6.2.0
  http: ^1.2.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
